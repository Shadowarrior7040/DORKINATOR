// Initialize tool
function init() {
    populateDorks();
    loadHistory();
}

// Populate dork list
function populateDorks() {
    const engine = document.getElementById('search-engine').value;
    const listDiv = document.getElementById('dork-list');
    // ... (code to display dorks)
}